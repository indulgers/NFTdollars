import mitt from 'mitt';

const emitter = mitt();
function useBaseMapListener() {
    return {
        emit: emitter.emit,
        on: emitter.on,
        off: emitter.off
    };
}

export { useBaseMapListener as default };
