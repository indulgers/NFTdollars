/**
 * warn 抛出警告
 * @internal
 * @param {string} 警告内容
 */
function warn(text) {
    console.warn(`[Vue3 BaiduMap GL] ${text}`);
}
function error(text) {
    console.error(`[Vue3 BaiduMap GL] ${text}`);
}

export { error, warn };
